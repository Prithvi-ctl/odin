Components-> reusable components  such as nav bar, footer and such, 

	rule for component -> the starting letter must be capital 
		greeting.jsx -> ╳
		Greeting.jsx -> ㊣

Exports and imports:
	You can only have one default export
	you can do this export let v1 = SoilderSerum();
	and also can do exort { name1,name2};
	